# ===================================
# Notion OAuth Configuration (NEW)
# ===================================
# Get these from your Notion OAuth integration settings at:
# https://www.notion.so/my-integrations
NOTION_CLIENT_ID=2a8d872b-594c-8028-bdc0-00370dfb7207
NOTION_CLIENT_SECRET=your-notion-client-secret-here
NOTION_REDIRECT_URI=http://localhost:3000/api/auth/callback

# Frontend public URL (used for OAuth redirects after Notion callback)
# Local: http://localhost:3000
# Production: https://n2n.thibaultspriet.fr (or your domain)
FRONTEND_URL=http://localhost:3000

# JWT secret for session tokens (change in production!)
# Generate a secure random string: openssl rand -hex 32
JWT_SECRET=29db62c97b34e5156bfeb4a6bfd82d121c1afa20f9ceedd4ef160de97eca7c1c

# ===================================
# OpenAI Configuration
# ===================================
# Use either standard OpenAI OR Azure OpenAI (not both)
OPENAI_API_KEY=

# ===================================
# Frontend Configuration
# ===================================
# Used by docker-compose for variable substitution
# APP_ENV: Set to 'development' to show test mode toggle, 'production' to hide it
# API_HOST: Use 'localhost' or your local IP address for local development, or your VPS IP/domain for production
# NOTION_CLIENT_ID and NOTION_REDIRECT_URI (defined above) are passed as build arguments to the frontend
APP_ENV=production
API_HOST=localhost

# ===================================
# Database Configuration
# ===================================
# MySQL Configuration (for local development and production)
# The ?charset=utf8mb4 is CRITICAL for proper emoji and Unicode support

# Local Development (docker-compose.yml):
DATABASE_URL=mysql+pymysql://notes2notion:notes2notion@mysql:3306/notes2notion?charset=utf8mb4

# Dokploy Production (configured in Dokploy UI):
# DATABASE_URL=mysql+pymysql://{user}:{password}@{mysql-service}:3306/notes2notion?charset=utf8mb4

# MySQL Service Configuration (for local docker-compose only)
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=notes2notion
MYSQL_USER=notes2notion
MYSQL_PASSWORD=notes2notion

# Legacy SQLite (deprecated):
# DATABASE_URL=sqlite:///./notes2notion.db