NOTION_TOKEN=
NOTION_PAGE_ID=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
OPENAI_API_KEY=

# Security - Access Code for API authentication
# This code will be required in the Authorization header for all API requests
# Choose a strong, random code (minimum 16 characters recommended)
# Example: openssl rand -hex 32
ACCESS_CODE=your-secret-access-code-here

# Frontend configuration (used by docker-compose for variable substitution)
# APP_ENV: Set to 'development' to show test mode toggle, 'production' to hide it
# API_HOST: Use 'localhost' or your local IP adress for local development, or your VPS IP/domain for production
APP_ENV=production
API_HOST=localhost