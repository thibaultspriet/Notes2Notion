services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: notes2notion-backend
    expose:
      - "5001"
    env_file:
      - .env
    environment:
      - BACKEND_PORT=5001
      - FLASK_ENV=development
      - ACCESS_CODE=${ACCESS_CODE}
    volumes:
      - ./backend/uploads:/app/uploads
      - ./src:/app/src
      - /var/run/docker.sock:/var/run/docker.sock  # For MCP Docker access
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5001/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.backend.entrypoints=websecure"
      - "traefik.http.routers.backend.rule=Host(`notes2notion-api-fullstack-du42m1-d3baa6-72-61-164-187.traefik.me`)"
      - "traefik.http.routers.backend.tls.certResolver=letsencrypt"
      - "traefik.http.services.backend.loadbalancer.server.port=5001"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - API_HOST=${API_HOST:-localhost}
        - APP_ENV=${APP_ENV:-production}
    container_name: notes2notion-frontend
    expose:
      - "3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_APP_ENV=${APP_ENV:-production}
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frontend.entrypoints=websecure"
      - "traefik.http.routers.frontend.rule=Host(`notes2notion-fullstack-du42m1-d3baa6-72-61-164-187.traefik.me`)"
      - "traefik.http.routers.frontend.tls.certResolver=letsencrypt"
      - "traefik.http.services.frontend.loadbalancer.server.port=3000"

networks:
  dokploy-network:
    external: true
